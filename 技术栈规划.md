# 地理五行·道法职业：设计方案与技术栈整合规划


本文将《地理五行·道法职业》的**设计要点**与**技术栈选型**整合成一份可执行的落地方案，并针对**单机/局域网联机**的成本约束进行调整。整体以**客户端优先、弱服务端依赖**为原则，覆盖客户端、数据层、地图与定位、局域网联机与事件系统、运营与安全等关键模块，确保“地理位置 + 阴阳五行 + 节气/天时”的玩法能够稳定实现。

---

## 1. 设计要点摘要（从方案落地出发）

### 1.1 核心玩法
- 基于**真实地理位置**生成职业的主五行/副五行与特殊属性。
- 结合**节气、天时与阴阳**变化驱动成长与事件。
- 支持**虚拟位置/旅行记忆点**，兼顾隐私与无障碍体验。

### 1.2 关键系统
- 地理触发探索与灵脉节点。
- 阴阳动态战斗与阵法联动。
- 门派/社交系统与区域绑定。

---

## 2. 客户端（多端一致体验）

### 2.1 建议技术
- **Unity 2022 LTS**（C#）
  - 优点：跨平台（iOS/Android/PC/WebGL）、成熟的渲染/动画/粒子体系、生态插件丰富。
- **UI 框架**：Unity UI Toolkit 或 UGUI
- **地图/地理展示**：
  - **国内地图服务**：高德地图 SDK / 百度地图 SDK / 腾讯位置服务（优先国内合规）
  - **成本控制**：优先选择免费额度高、低成本调用的 SDK；配合离线瓦片缓存与简化地形渲染
- **定位与传感器**：
  - iOS Core Location / Android Fused Location Provider
  - 支持“虚拟位置/旅行记忆点”模式

### 2.2 客户端关键模块
- 位置采样与隐私管理
- 五行职业生成结果的本地展示
- 实时地图/地形事件触发
- 战斗与阵法系统（前端驱动，数据由服务端校验）

---

## 3. 单机/局域网联机架构原则

### 3.1 是否需要服务端支持？
- **纯单机模式**：不需要服务端，所有规则与存档均在本地完成。
- **局域网联机模式**：不需要公网服务端，可采用**房主主机（Host）+ 客户端**的本地同步架构。
- **可选轻量服务端**：仅在需要云存档、跨设备同步或远程活动时引入；可后期增量接入。

### 3.2 架构取舍
- 将**职业生成、战斗结算、阵法联动**等核心规则统一放在客户端执行。
- 联机时由房主主机作为权威结算点（权威客户端），其他玩家进行状态同步。
- 运营与活动配置采用**本地配置包 + 可选在线更新**策略，避免强依赖后端。

---

## 4. 服务端（可选，轻量化）

### 4.1 建议技术（可选）
- **语言/框架**：
  - **Go + Gin/Fiber**（仅在需要云存档/在线活动时启用）
- **鉴权**（可选）：JWT + OAuth2（支持游客/第三方登录）
- **API 协议**（可选）：REST + WebSocket（远程活动/同步）

### 4.2 关键服务模块（可选）
- **云存档与账号服务**：跨设备同步与恢复
- **活动配置与内容更新**：节气活动/灵脉节点的热更新
- **统计与日志**：本地或云端聚合

---

## 5. 数据与存储

### 5.1 数据库/本地存储
- **本地存档**：SQLite / LiteDB / Unity Serialization
- **地理数据缓存**：本地瓦片缓存 + 轻量索引
- **可选服务端数据库**：PostgreSQL + PostGIS（仅在启用云存档/在线活动时）
- **缓存**：Redis（可选）

### 5.2 数据模型重点
- 地理区域 → 五行权重表
- 玩家位置/旅行记忆点 → 职业生成记录
- 灵脉节点与地脉事件 → 生命周期状态

---

## 6. 地图与地理数据

### 6.1 地图数据源
- **国内地图服务优先**：高德地图 / 百度地图 / 腾讯位置服务
- 关键数据：
  - 地形类型（森林、水域、火山、城市、矿脉）
  - 高程/气候/季节

### 6.2 五行映射计算
- 基于地形分类 + 地理特征权重映射到五行
- 节气/天气修正：影响木火土金水比重

---

## 7. 实时系统与事件驱动（局域网）

- **局域网联机**：UNet 替代方案（Mirror/Photon Fusion LAN）
- **本地事件总线**：客户端内事件驱动（不依赖 MQ）
- **可选远程事件**：仅在启用在线活动时使用 WebSocket/Socket.IO

---

## 8. 运营与配置系统

- **GM 后台**：活动配置、节气事件、灵脉节点刷新规则
- **配置中心**：
  - 五行权重、特殊属性触发概率
  - 职业成长曲线与数值平衡

---

## 9. 客户端安全与反作弊

- 位置校验（GPS 可信度检测 + 行为规则校验）
- 异常移动识别（速度/跨区域跳跃）
- 单机裁决：职业生成与战斗结算在本地执行（可选联机时由房主主机裁决）

---

## 10. 部署与运维

- **单机/局域网模式**：无需复杂运维，仅需版本发布与配置更新
- **可选云服务**：Docker + 轻量化部署（仅在启用云存档/在线活动时）

---

## 11. 可选拓展

- **AR/实景互动**：ARCore / ARKit
- **AI NPC**：对话型 NPC 交互（LLM 服务 + 缓存）
- **UGC 阵法**：玩家上传阵法模板并进行审核

---

## 12. 推荐技术栈汇总（按模块）

| 模块 | 推荐技术 |
| --- | --- |
| 客户端 | Unity 2022 LTS、C#、UGUI/UI Toolkit |
| 地图 | 高德地图 SDK / 百度地图 SDK / 腾讯位置服务 |
| 定位 | iOS Core Location / Android Fused Location |
| 服务端（可选） | Go（Gin/Fiber） |
| 数据库 | 本地 SQLite / Unity Serialization（可选：PostgreSQL + PostGIS） |
| 实时 | 局域网联机（Mirror/Photon Fusion LAN） |
| 运维 | 单机/局域网发布（可选：Docker 轻量化部署） |

---

**说明**：以上技术栈可根据团队规模与预算调整。若以单机/局域网模式为首发目标，可优先落地 Unity + 本地存档（SQLite/Serialization）+ 国内地图 SDK 的组合，快速验证“位置 + 五行”的核心玩法。
